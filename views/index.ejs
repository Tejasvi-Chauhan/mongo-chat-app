<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All chats</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
   <a href="http://localhost:8080/chats/new" target="_blank">Create new chat</a>
   <br>
   <br>
   
    <% for(let chat of chats){ %>
        <div class="chat">    
        <p>From : <i><%=chat.from %></i></p>
         <p class="msg"> <%=chat.msg %></p>
          <p>Recieved by :<i> <%=chat.to %></i></p>
          
          
          <p><%=chat.created_at.toString().split(" ")[4]%></p>
          <p><%=chat.created_at.toString().split(" ").splice(0,4)
          .join(" ")%></p>
          
           <a href="/chats/<%= chat._id %>/edit" target="_blank">Edit</a>

           <form action="/chats/<%= chat._id %>?_method=DELETE" method="POST" >
    <button >Delete</button>
</form>


         
        </div>
   <% } %> 
</body>
</html>